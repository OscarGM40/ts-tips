// https://www.youtube.com/watch?v=G53cgl3h5Hc

// Básicamente un decorador es una función, que agregará comportamiento a la entidad que decore, modificandolo, extendiendolo,etc Puede recibir parametros. Si recibe parametros se le llama decorator factory, sino es un decorador normal
//! Para poder usar decoradores hay que tener la prop 'experimentalDecorators' a true en el tsconfig.json

// Decoradores de clase: estos decoradores se caracterizan por decorar las clases
export class UserRepository


// Decoradores de método
// Decoradores de propiedades/instancias de clases, campos de clase

// Decoradores de accesors y parametros
